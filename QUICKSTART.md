# Быстрый старт Elenya Bot

## Шаг 1: Установка зависимостей

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows

# Установить зависимости
pip install -r requirements.txt
```

## Шаг 2: Настройка токенов

Убедитесь, что файл `.env` существует и содержит:

```env
TELEGRAM_TOKEN=ваш_токен_телеграм
OPENAI_API_KEY=ваш_ключ_openai
```

### Как получить токены:

**Telegram Bot Token:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

**OpenAI API Key:**
1. Зайдите на [platform.openai.com](https://platform.openai.com)
2. Перейдите в API Keys
3. Создайте новый ключ
4. Скопируйте его (он показывается только один раз!)

## Шаг 3: Проверка настройки

Запустите тестовый скрипт:

```bash
python test_setup.py
```

Если все настроено правильно, вы увидите:
```
✅ Config загружен
✅ Telegram токен найден
✅ OpenAI ключ найден
✅ Словарь найден
✅ Все зависимости установлены
```

## Шаг 4: Запуск бота

```bash
python bot.py
```

При первом запуске бот загрузит словарь (это займет 10-20 секунд):

```
🚀 Запуск бота Elenya...
📚 Загрузка словаря Elenya...
✅ Словарь загружен: X фрагментов
⚙️  Инициализация сервисов...
✅ Бот запущен и готов к работе!
```

## Шаг 5: Тестирование

1. Найдите вашего бота в Telegram по имени, которое вы дали при создании
2. Отправьте `/start`
3. Выберите режим работы
4. Попробуйте отправить:
   - Текст: "звезда"
   - Голосовое сообщение
   - Фото любого объекта

## Решение проблем

### Ошибка: "TELEGRAM_TOKEN не найден"
- Проверьте, что файл `.env` находится в корне проекта
- Убедитесь, что в нем есть строка с токеном без пробелов

### Ошибка: "No module named 'telegram'"
- Активируйте виртуальное окружение
- Запустите `pip install -r requirements.txt`

### Ошибка: "elenya_dict.pdf not found"
- Убедитесь, что файл `elenya_dict.pdf` находится в `rag/data/`

### Бот не отвечает
- Проверьте, что бот запущен (видите "Бот запущен")
- Убедитесь, что используете правильное имя бота
- Проверьте интернет-соединение

## Полезные команды

```bash
# Остановить бота
Ctrl + C

# Обновить зависимости
pip install --upgrade -r requirements.txt

# Очистить кеш Python
find . -type d -name "__pycache__" -exec rm -rf {} +

# Посмотреть логи (если настроены)
tail -f bot.log
```

## Структура проекта

```
elf_lang/
├── bot.py           # Запустите этот файл
├── config.py        # Конфигурация
├── .env             # Ваши токены (не коммитить!)
├── handlers/        # Обработчики сообщений
├── services/        # Бизнес-логика
├── utils/           # STT, Vision
└── rag/             # RAG система
    └── data/        # Данные
        └── elenya_dict.pdf  # Словарь
```

---

Если возникли проблемы, проверьте README.md для полной документации.
